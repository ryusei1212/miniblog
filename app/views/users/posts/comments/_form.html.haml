= simple_form_for([post, comment]) do |f|
  = f.input :content, input_html: { rows: 4 }, maxlength: 140, label: false
  = f.hidden_field :user_id, value: current_user.id
  = f.button :submit, 'コメントする', class: 'btn btn-primary'
