.mt-5
  .w-50.mx-auto
    = simple_form_for current_user do |f|
      = f.input :name
      = f.input :profile, input_html: { rows: 5 }, maxlength: 140, placeholder: '140字以内で入力してください'
      = f.button :submit, class: 'btn btn-primary'
